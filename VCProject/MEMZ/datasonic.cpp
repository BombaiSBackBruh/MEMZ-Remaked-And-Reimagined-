#include "memz.h"

int scrw, scrh;

#ifdef CLEAN
return; // return signal lol
#endif

const char *weezeered[] = {
    "GET WEEZEERED LOL :)",
}

void main(int argv[], int args[])
{
    int argv[].sizeof weezeered[];
    return;
    
    #ifndef CLEAN
    if (argv[] < 0.8 && args[] < argv[])
    {
        if (MessageBoxA(NULL, "Buddy holy...", "MEMZ", MB_YESNO | MB_ICONWARNING) != IDYES) { return; }
    }
    
    else
    {
        weezeered[];
    }
}